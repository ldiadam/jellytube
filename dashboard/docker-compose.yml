services:
  youtube-dashboard:
    build: .
    container_name: youtube-dashboard
    restart: unless-stopped
    ports:
      - "8088:8080"
    volumes:
      - /media/sdb1/YouTube:/youtube
      - /media/sdb1/YouTube-dashboard:/config
